# -*- ruby -*-

require 'rubygems'
require 'hoe'
require './lib/ec2_mysql.rb'

Hoe.new('Ec2Mysql', Ec2Mysql::VERSION) do |p|
  # p.rubyforge_name = 'Ec2Mysqlx' # if different than lowercase project name
  # p.developer('FIX', 'FIX@example.com')
end

# vim: syntax=Ruby
